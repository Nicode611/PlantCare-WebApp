import React from 'react'

export default function myPlantsSection() {
    return (
        <section 
            className="w-full md:w-[80%] flex justify-center items-center p-2 z-50 backdrop-blur-md bg-secondary bg-opacity-20"
        style={{ overscrollBehaviorX: "contain", backdropFilter: "blur(5px)" }}
        >
            {/* Contenu de la section */}
        </section>
    )
}
